name: Missing PR Sonar scope
on:
  pull_request:
permissions:
  contents: read
jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Sonar published result
        run: just sonar-verify-result
